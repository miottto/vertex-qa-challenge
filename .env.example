# Database Configuration Example
# Copy this file to .env if you want to customize your local environment.
# If .env is missing, the application defaults to these values automatically.

DB_USER=admin
DB_PASS=password123
DB_NAME=vertex_db
DB_HOST=localhost
DB_PORT=5432